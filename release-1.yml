# Starter pipeline
trigger:
  - master

pool:
  vmImage: ubuntu-latest

steps:
- task: DownloadBuildArtifacts@1
  inputs:
    buildType: 'specific'
    project: 'd4cb9f73-8cc5-477d-8c52-564663d622e4'
    pipeline: '1'
    buildVersionToDownload: 'latest'
    downloadType: 'specific'
    downloadPath: '$(System.ArtifactsDirectory)'

